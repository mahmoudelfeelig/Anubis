# lv-004 - Lobby Splice

Treat clock times as directions from the center of each grid. In grid A, west, north, east, center, southeast gives clock. In grid B, north, east, south gives cut.

Final credentials:

- Username: `clock`
- Password: `cut`
