### Flux Gallery

Three sealed boxes were scanned before the manifold locked. Each block lists a rectangular region and a sequence of vector fields. Operations only kept the divergence footprints—turn them back into letters by evaluating the outward flux of every field.

All integrals use outward orientation. Map `0 → a` when you translate the flux scalars into text.

**Block A — unit cube**  
\(R_A = \{(x,y,z) \mid 0 \le x,y,z \le 1\}\)

| Field | \(F(x,y,z)\) |
| ----- | ------------- |
| \(F_1\) | \((x,\;2y,\;2z)\) |
| \(F_2\) | \((2x,\;3y,\;6z)\) |
| \(F_3\) | \((5x^2,\;15y,\;0)\) |
| \(F_4\) | \((8x^2,\;5y^2,\;10z)\) |

**Block B — doubled slab**  
\(R_B = \{(x,y,z) \mid 0 \le x,z \le 1,\; 0 \le y \le 2\}\)

| Field | \(F(x,y,z)\) |
| ----- | ------------- |
| \(G_1\) | \((4x,\;0,\;0)\) |
| \(G_2\) | \((4x,\;\tfrac{5}{4}y^2,\;0)\) |
| \(G_3\) | \((\tfrac{7}{2}x + \tfrac{5}{2}x^2,\;\tfrac{3}{2}y^2,\;\tfrac{1}{2}z^2)\) |
| \(G_4\) | \((2x,\;0,\;0)\) |

**Block C — column**  
\(R_C = \{(x,y,z) \mid 0 \le x,y \le 1,\; 0 \le z \le 2\}\)

| Field | \(H(x,y,z)\) |
| ----- | ------------- |
| \(H_1\) | \((3x,\;0,\;0)\) |
| \(H_2\) | \((5x + \tfrac{5}{2}x^2,\;y^2,\;0)\) |
| \(H_3\) | \((-y,\;x,\;0)\) |
| \(H_4\) | \((\tfrac{7}{2}x,\;\tfrac{3}{2}y^2,\;\tfrac{1}{4}z^2)\) |
