# LV-009 – Null Proxy

1. Convert both hex streams to bytes and XOR them to recover the payload `GET /dnsghost/xorpath HTTP/1.1`.
2. Extract the path segment after the slash.
3. Username `dnsghost`, password `xorpath`.
